# the currently active colorscheme will be at the top.
# i've kept some of the other colorschemes and configs 
# i liked below it for reference.

{
  colorschemes = {

    gruvbox.enable = true;

    vscode = {
      enable = false;

      settings = {
        italic_comments = true;
        underline_links = true;
        disable_nvimtree_bg = false;
      };
    };

    everforest = {
      enable = false;

      settings = {
        background = "hard";
        italic_comments = true;
        underline_links = true;
      };
    };

  };
}
